import react from 'react';
const jsxintro=()=>{
  const topic="jsx"

return(
  <div>
    <h1>welcome to {topic}</h1>
    <p>{topic}short for JavaScript XML, is a syntax extension for JavaScript that enables developers to write HTML-like code directly within their JavaScript files</p>
  </div>
);
};
export default jsxintro;